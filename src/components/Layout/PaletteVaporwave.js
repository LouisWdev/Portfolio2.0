import { createTheme, createThemePaletteBasic } from '@arwes/design';


  const PaletteVaporwave = createTheme({
    palette: {

      VaporwavePurple: createThemePaletteBasic({ main: '#00f' })}
  });


export default PaletteVaporwave